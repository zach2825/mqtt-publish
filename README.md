The only thing this package has is a mqtt integration

### SETUP
 
 Run  `composer install`
 
copy the .env.example to .env

set the mqtt options at the bottom of the file

### Example usage
```
php artisan homeauto:commander office/desk/fan 1
```
